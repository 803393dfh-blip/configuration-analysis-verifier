# Copy this file to .env and fill real values before running in real (non-mock) mode.

# GitHub token with repo and actions read access (format: ghp_xxx...)
MCP_GITHUB_TOKEN=ghp_xxx_replace_me

# Repository owner (user or org)
REPO_OWNER=your-github-organization-or-username

# Repository name (the repo that contains analysis_results.json)
REPO_NAME=your-repo-name

# Directory where verification report will be written (relative to repo root)
# Default in script is "project_docs"
PROJECT_DOCS_DIR=project_docs
